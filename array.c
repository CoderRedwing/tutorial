#include<stdio.h>
int main ()
{
    int big=0,big1=0,temp=0,a[6],i;
    printf("enter the the element of the array:");
    for( i=0; i<6; i++)
    {
        scanf("%d",&a[i]);
    }
    for( i=0; i<6; i++)
    
    printf("  %d  ",a[i]);
    
    return 0;
}